apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: markitdown-storage
  labels:
    app: markitdown-mcp
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ""  # Use default storage class or specify your own
